# True-Tone Requirements

# Core audio/data processing
numpy
scipy
librosa
soundfile

# Live audio capture
sounddevice
soundcard

# AI/audio inference
torch
torchaudio
transformers
silero-vad

# UI/dashboard
streamlit
matplotlib

# Utilities
python-dotenv

# Notes:
# - tkinter is included with most Windows Python installs and is not a pip package.
# - pyaudio is optional fallback capture support. On Windows/Python 3.14 it may require
#   Microsoft C++ Build Tools because a compatible wheel may not be available.
